Simple telegram bot to handle local transmission instances. Simply send the url, magnet o torrent file and the bot will start the download.

To install create a file called instanceElements.py, the USERS list contains the ids of authorized users:

	# instanceElements.py
	TOKEN="..."
	USERS=[...] # optional
